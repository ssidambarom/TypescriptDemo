
var simple: string = 'string simple quote'
var double: string = "string avec double quote";

var version: number = 1;
var template: string = `Mon super template version: ${version}

Cool!`;

var equivalentTemplate: string = 'Mon super template version: '+ version 
                                + '\n\n' + 'Cool!';